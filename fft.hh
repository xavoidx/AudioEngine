#include <vector>
#include <complex>
#include <cmath>
#include <math.h>
#include <bit>
#include <iostream>
typedef std::complex<double> cd;
std::vector<cd> dft(std::vector<double>&);
void fft(std::vector<std::complex<double>>&);

